# stage-0-foundations

Everything that has to exist before anything else can start. Nothing flies in this stage.

Done when the repo runs, the hardware is characterised, and the two numbers almost everything
downstream depends on — the radio ceiling and the control lag — are measured rather than guessed.

Exit test: 0.5 stage-0-runbook.
